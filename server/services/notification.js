function notify(){
    const notifyy               = ()=>{
        console(console.error();
        )
        function message(){
            for (let index = 0; index < array.length; index++) {
                const element = array[index];
                
            }
        }
        console(error)
    }
}